apiVersion: v1
kind: Service
metadata:
  name: product-scanner-dashboard
  namespace: api
  labels:
    app: product-scanner-dashboard
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: product-scanner-dashboard
